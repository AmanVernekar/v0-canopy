# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables — broad rule so any .env, .env.local,
# .env.production, etc. is ignored. Only .env.example is committed.
.env
.env.*
!.env.example

# Common ignores
node_modules
.next/
.DS_Store

# Local data + tooling
raw/
.mypy_cache/
.dmypy.json

# Project-private spec — local source of truth, not for the deployed repo
context.md
.claude/